<UserControl x:Class="MyToDoList.Views.LoginPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDoList.Views"
             xmlns:res="clr-namespace:MyToDoList.Resources"
             mc:Ignorable="d" 
             Height="450" Width="200"
             >
    <StackPanel Orientation="Vertical" >
        <TextBlock Text="{x:Static res:ConstantStrings.Login}" Height="20px" FontWeight="Bold" />
        <TextBlock Text="{x:Static res:ConstantStrings.Username}"/>
        <TextBox Text="{Binding Username}"/>
        <TextBlock Text="{x:Static res:ConstantStrings.Password}" />
        <TextBox Text="{Binding Password}" />
        <TextBlock Text="" />
        <Button Margin="4" Content="{x:Static res:ConstantStrings.Login}" Command="{Binding Login}" />
        <TextBlock Margin="4" Text="{x:Static res:ConstantStrings.NoAccountYet}">
            <Hyperlink Command="{Binding Signup}" >
                <Run Text="{x:Static res:ConstantStrings.Signup}"/>
            </Hyperlink>
        </TextBlock>
    </StackPanel>
</UserControl>
