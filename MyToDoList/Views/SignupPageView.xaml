<UserControl x:Class="MyToDoList.Views.SignupPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDoList.Views"
             xmlns:res="clr-namespace:MyToDoList.Resources"
             mc:Ignorable="d" 
             Height="450" Width="200"
             >
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    
    <StackPanel Orientation="Vertical" >
        <TextBlock Text="{x:Static res:ConstantStrings.Signup}" Height="20px" FontWeight="Bold" />
        <TextBlock Text="{x:Static res:ConstantStrings.Name}"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="{x:Static res:ConstantStrings.Surname}"/>
        <TextBox Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="{x:Static res:ConstantStrings.Email}"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="{x:Static res:ConstantStrings.Username}"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="{x:Static res:ConstantStrings.Password}" />
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{Binding ErrorMessage}" Visibility="{Binding IsVisibleError, Converter={StaticResource BoolToVis}}" />
        <Button Margin="4" Content="{x:Static res:ConstantStrings.Signup}" Command="{Binding Signup}" IsEnabled="{Binding IsEnabledSignup}" />
        <TextBlock Margin="4" Text="{x:Static res:ConstantStrings.IHaveAccountAlready}">
            <Hyperlink Command="{Binding Login}" >
                <Run Text="{x:Static res:ConstantStrings.Login}"/>
            </Hyperlink>
        </TextBlock>
    </StackPanel>
</UserControl>
